# A list can be modified, and more elements can be added to an existing list. 
# Use the append(x) function to add some more items to the list to produce the same content shown in the sample below.
aList = ['Hello', 0,20.0,'World'] 
aList.append("string")
print(aList)